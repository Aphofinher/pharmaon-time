import {StyleSheet, View } from "react-native";

const MenuAncora = () => {
  return <View style={styles.menuAncora} />;
};

const styles = StyleSheet.create({
  menuAncora: {
    flex: 1,
    width: "100%",
    height: 800,
    overflow: "hidden",
  },
});

export default MenuAncora;
